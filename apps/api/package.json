{
  "name": "@studentconnect/api",
  "version": "0.1.0",
  "private": true,
  "description": "StudentConnect Backend - FastAPI",
  "type": "module",
  "scripts": {
    "dev": "../../.venv/bin/python -m uvicorn app.main:app --reload --port 8000",
    "build": "echo 'No build step for FastAPI'",
    "start": "../../.venv/bin/python -m uvicorn app.main:app --port 8000",
    "lint": "../../.venv/bin/python -m ruff check .",
    "typecheck": "../../.venv/bin/python -m mypy .",
    "test": "../../.venv/bin/python -m pytest",
    "format": "../../.venv/bin/python -m ruff format .",
    "clean": "rm -rf __pycache__ *.pyc .venv",
    "db:upgrade": "../../.venv/bin/python -m alembic upgrade head",
    "db:migrate": "../../.venv/bin/python -m alembic revision --autogenerate -m"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "python": ">=3.11.0"
  }
}
